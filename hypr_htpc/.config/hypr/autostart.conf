# Autostart
exec-once = swaybg -i ~/Pictures/wallpaper.jpg -m fill
exec-once = ~/.config/waybar/scripts/waybar-start.sh
# Sunshine KMS capture conflicts with wayvnc on this HTPC.
# exec-once = sleep 8 && systemctl --user restart sunshine.service

# NVIDIA HDR/MetaMode forcing disabled while VNC/screenshare is in use.
# exec-once = sleep 2 && nvidia-settings -a CurrentMetaMode="HDMI-A-1: 1920x1080_60 +0+0 {AllowGSYNCCompatible=On}"
