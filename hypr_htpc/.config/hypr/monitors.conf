# LG OLED65C44LA - 1440p@120 with couch-friendly scale.
# Desktop stays SDR; fullscreen apps can request HDR via cm_auto_hdr.
monitor = HDMI-A-1, 2560x1440@120, 0x0, 1.25

render {
    cm_auto_hdr = 1
}

monitor = DVI-D-1, disable
