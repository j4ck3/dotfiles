# hyprpm needs the live HYPRLAND_INSTANCE_SIGNATURE (stale after Hyprland restart).
[misc]
disable = ["hyprpm", "config_update"]
show_distribution_summary = false

[commands]
"Hyprpm" = "hypr-sync-env run hyprpm update"
