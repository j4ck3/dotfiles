# Flatpak launcher reads $XDG_CONFIG_HOME/spotify-flags.conf (see /app/bin/spotify).
# Without this, Spotify picks /dev/dri/renderD128 and exits: "GPU process isn't usable".
--force-dark-mode
--render-node-override=/dev/dri/by-path/pci-0000:03:00.0-render
--ignore-gpu-blocklist
