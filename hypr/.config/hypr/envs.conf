# Extra env variables
# env = MY_GLOBAL_ENV,setting

# AMD / Hyprland stability defaults.
# Leave HDR-related flags off until the basic display path is stable.
# If you still get a black screen after the monitor fix below, test safe mode
# first and only then try the fallback below from a TTY:
# env = AQ_NO_ATOMIC,1
