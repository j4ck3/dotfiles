# CS2 on Linux is SDL→X11→XWayland (see game/cs2.sh). Floating window often breaks capture.

windowrule {
  name = cs2-fullscreen
  match:class = ^cs2$

  fullscreen = 1
  no_anim = on
}

# Steam launch options (Properties → Launch Options), try in order if aim still wrong:
#   SDL_VIDEO_DRIVER=wayland %command%
#   gamescope -f -- %command%   (after: sudo pacman -S gamescope)
